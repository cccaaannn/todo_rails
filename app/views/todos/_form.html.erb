<div class="form-container">
  <%= form_with(model: todo) do |form| %>
    <% if todo.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(todo.errors.count, "error") %> prohibited this todo from being saved:</h2>

        <ul>
          <% todo.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= form.text_area :task, class: "todo-input" %>

    <div class="todo-form-bottom">
      <%= form.select :priority, options_for_select([['🔴', 'High'], ['🟠', 'Medium'], ['🟡', 'Low']], selected: todo.priority) %>

      <%= form.submit "Save", class: "todo-submit" %>
    </div>
  <% end %>
</div>
